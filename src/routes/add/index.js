import { Button, ButtonGroup } from '@urban-bot/core';
import React from 'react';

const Add = () => {

    return(
        <ButtonGroup title="Че надо">
            <Button>First</Button>
            <Button>Second</Button>
        </ButtonGroup>
    )
}

export default Add;